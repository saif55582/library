extends layout

block content

    h1 #[ Author : ] #{author.name}
     

    div(style='margin-left:20px;margin-top:20px',class='well')
        h4 Books

        dl
        each book in author_books
            hr
            dt 
                a(href=book.url) #{book.title}
            dd #{book.summary}
        else
            p This author has no books

        hr
        p 
            a(href=author.url+'/delete') Delete Author